[
  {
    "properties": {
      "ignorePattern": {
        "type": "string",
        "description": "Pattern (JavaScript syntax) for text of trailing comments that are allowed.",
        "default": "^\\s*[^\\s]+$",
        "title": "pattern"
      }
    }
  }
]
