[{
  "type": "object",
  "properties": {
    "ignore": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "default": [ 0, 1, -1, 24, 60 ],
      "description": "An array of numbers to ignore. It’s set to [] by default. If provided, it must be an Array.",
      "uniqueItems": true
    },
    "ignoreDefaultValues": {
      "type": "boolean",
      "default": true,
      "description": "A boolean to specify if numbers used in default value assignments are considered okay."
    },
    "ignoreClassFieldInitialValues": {
      "type": "boolean",
      "default": true,
      "description": "A boolean to specify if numbers used as initial values of class fields are considered okay."
    }
  },
  "additionalProperties": false
}]
