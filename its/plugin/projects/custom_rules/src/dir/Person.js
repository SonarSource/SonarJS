/** BaseTreeVisitor **/
var Person = function(first, last, middle) {
    this.first = first;
    this.middle = middle;
    this.last = last;
};

/** SubscriptionBaseVisitor **/
var myArray = [1, 2, 3];

for (props in myArray) {
  // do something;
}
