{
  "name": "eslint-plugin-sonarjs-tests",
  "description": "Test suite for eslint-plugin-sonarjs",
  "scripts": {
    "build": "npm ci && cp ../../packages/jsts/src/rules/lib/eslint-plugin-sonarjs-* ./plugin.tgz && ls -la && npm i ./plugin.tgz --no-save && rm -f plugin.tgz",
    "test": "node index.test.js"
  },
  "devDependencies": {
    "cross-spawn": "^7.0.3",
    "eslint": "8.57.0"
  }
}
