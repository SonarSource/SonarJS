AWSTemplateFormatVersion: 2010-09-09
Resources:
  SomeLambdaFunction:
    Type: "AWS::Serverless::Function"
    Properties:
      Runtime: "nodejs16.0"
      InlineCode: for (var i = 0; i < strings.length; i--) {}
