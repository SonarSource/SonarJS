export type FakeExternalType = string | undefined;
export type FakeUnionWithoutUndefined = string | number;

export interface FakeExternalProperties {
  optional?: string;
  required: string;
}

// Two shapes of index signature: an explicit one, which has an
// `IndexSignatureDeclaration` node, and a mapped type, whose index info is
// synthesized by the checker and has no declaration.
export interface FakeExternalIndexed {
  [key: string]: string | undefined;
}
export type FakeExternalRecord = Record<string, string | undefined>;

export namespace FakeNamespace {
  export type Nested = string | undefined;
}
