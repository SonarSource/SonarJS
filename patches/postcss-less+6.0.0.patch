diff --git a/node_modules/postcss-less/lib/index.js b/node_modules/postcss-less/lib/index.js
index 42048db..c0e0427 100644
--- a/node_modules/postcss-less/lib/index.js
+++ b/node_modules/postcss-less/lib/index.js
@@ -37,6 +37,9 @@ module.exports = {
       const newStartOffset = offset + node.source.start.offset;
       const newStartPosition = input.fromOffset(offset + node.source.start.offset);

+      // eslint-disable-next-line no-param-reassign
+      node.source.input = input;
+
       // eslint-disable-next-line no-param-reassign
       node.source.start = {
         offset: newStartOffset,
